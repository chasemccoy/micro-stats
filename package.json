{
  "name": "micro-stats",
  "version": "1.0.0",
  "description": "A tiny Node.js microservice to grab some stats from around the web.",
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "dev": "micro-dev",
    "deploy": "now && now alias",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write '*.js'"
  },
  "repository": "git@github.com:chasemccoy/micro-stats.git",
  "author": "Chase McCoy <chasem000@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^6.2.0",
    "micro": "^9.3.3",
    "node-fetch": "^2.3.0",
    "now-micro": "^0.4.0",
    "twitter-lite": "^0.9.0"
  },
  "devDependencies": {
    "micro-dev": "^3.0.0",
    "prettier": "^1.18.2"
  },
  "engines": {
    "node": "10.x"
  }
}
